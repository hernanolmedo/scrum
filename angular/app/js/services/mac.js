angular.module('scrum.services.mac', [])
.factory('Mac', ['ResourceFactory', function (ResourceFactory) {
	
	// // Recurso local
	// var Mac = ResourceFactory('Mac', 'macs'); // Nombre del recurso, Nombre del recurso en API (URL)

	// // Dev only
	// window.Mac = Mac;

	// // Se expone el servicio
	// return Mac;
}]);